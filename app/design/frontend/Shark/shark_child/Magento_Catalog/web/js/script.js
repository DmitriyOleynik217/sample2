define([
    'jquery',
    'Magento_Catalog/js/widget',
    'domReady!'
],  function ($) {
        'use strict';

        return function (config, element) {
            $(element).customWidget();
            $(element).customWidget();
            $(element).customWidget();
            $(element).customWidget();
        };
    });
